[workspace]
resolver = "2"
members = ["crates/*", "examples/*", ]

[workspace.package]
version = "0.3.0"
edition = "2024"
license = "MIT OR Apache-2.0"
repository = "https://github.com/voxell-tech/motiongfx"

[workspace.dependencies]
# motiongfx
motiongfx = { version = "0.3.0", path = "crates/motiongfx" }
bevy_motiongfx = { version = "0.3.0", path = "crates/bevy_motiongfx" }
peniko_motiongfx = { version = "0.3.0", path = "crates/peniko_motiongfx" }

# bevy
bevy = { version = "0.18.1", default-features = false }
bevy_ecs = { version = "0.18.1", default-features = false }
bevy_app = { version = "0.18.1", default-features = false }
bevy_platform = { version = "0.18.1", default-features = false }
bevy_derive = { version = "0.18.1", default-features = false }
bevy_math = { version = "0.18.1", default-features = false, features = [ "libm" ] }
bevy_asset = { version = "0.18.1", default-features = false }
bevy_time = { version = "0.18.1", default-features = false }
bevy_transform = { version = "0.18.1", default-features = false, features = ["bevy-support"] }
bevy_color = { version = "0.18.1", default-features = false }
bevy_reflect = { version = "0.18.1", default-features = false }
bevy_remote = { version = "0.18.1", default-features = false }

# serialization
serde = { version = "1", default-features = false }
serde_json = { version = "1" }

# other
field_path = "0.4.1"
nonempty = { version = "0.12", default-features = false }
peniko = { version = "0.6", default-features = false }
vello = "0.7.0"
winit = "0.30"
pollster = "0.4"

[workspace.lints.clippy]
redundant_type_annotations = "warn"

# Enable a small amount of optimization in the dev profile.
[profile.dev]
opt-level = 1

# Enable a large amount of optimization in the dev profile for dependencies.
[profile.dev.package."*"]
opt-level = 3
